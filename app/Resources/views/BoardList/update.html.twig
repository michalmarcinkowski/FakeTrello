{% extends "layout.html.twig" %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusResourceBundle:Macros:actions.html.twig' import update %}

{% block body %}
    <div class="page-header">
        <h1><i class="glyphicon glyphicon-plus-sign"></i> {{ 'Edit list' }}</h1>
    </div>

    {{ form_errors(form) }}

    <form action="{{ path('app_board_list_update', {'boardId': board_list.board.id, 'id': board_list.id}) }}" method="post" class="form-horizontal" >
        <input type="hidden" name="_method" value="PUT">
        {% include ':BoardList:_form.html.twig' %}
        {{ update() }}
    </form>
{% endblock %}
